1. In BlueJ, Right click on AppController and call getInstance()
2. In the pop up window, choose GET
3. The object will appear at bottom of the screen
4. Invoke the controller's methods

Open package "test" to run unit tests